---
import BaseHead from '../components/BaseHead.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import { SITE_TITLE, SITE_DESCRIPTION } from '../consts';
import { getCollection } from 'astro:content';
import FormattedDate from '../components/FormattedDate.astro';

const projects = (await getCollection('projects')).slice(0, 2);
const posts = (await getCollection('blog'))
	.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf())
	.slice(0, 3);
---

<!doctype html>
<html lang="pt-BR">
	<head>
		<BaseHead title={SITE_TITLE} description={SITE_DESCRIPTION} />
	</head>
	<body>
		<Header />
		<main>
			<section class="hero">
				<div>
					<h1>Projetos prontos para rodar</h1>
					<p>
						Este site já vem com uma vitrine de projetos, posts e páginas configuradas para você
						publicar e manter tudo atualizado com facilidade.
					</p>
					<div class="cta">
						<a class="primary" href="/projects">Ver projetos</a>
						<a class="secondary" href="/blog">Ler o blog</a>
					</div>
				</div>
				<div class="highlight">
					<strong>Incluído:</strong>
					<ul>
						<li>Rotas de projetos com páginas detalhadas.</li>
						<li>Listagem pronta para portfolio.</li>
						<li>Estrutura de conteúdo validada.</li>
					</ul>
				</div>
			</section>

			<section class="section">
				<div class="section-header">
					<h2>Projetos em destaque</h2>
					<a href="/projects">Ver todos</a>
				</div>
				<div class="grid">
					{projects.map((project) => (
						<a class="card" href={`/projects/${project.id}/`}>
							{project.data.heroImage && (
								<img
									width={640}
									height={320}
									src={project.data.heroImage}
									alt=""
								/>
							)}
							<h3>{project.data.title}</h3>
							<p>{project.data.description}</p>
						</a>
					))}
				</div>
			</section>

			<section class="section">
				<div class="section-header">
					<h2>Últimos posts</h2>
					<a href="/blog">Ver o blog</a>
				</div>
				<ul class="posts">
					{posts.map((post) => (
						<li>
							<a href={`/blog/${post.id}/`}>
								<h4>{post.data.title}</h4>
								<p>{post.data.description}</p>
								<span>
									<FormattedDate date={post.data.pubDate} />
								</span>
							</a>
						</li>
					))}
				</ul>
			</section>
		</main>
		<Footer />
		<style>
			.hero {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
				gap: 2rem;
				align-items: start;
			}
			.hero .highlight {
				padding: 1.5rem;
				border-radius: 16px;
				background: rgba(var(--accent), 8%);
				color: rgb(var(--gray-dark));
			}
			.hero ul {
				margin: 0.75rem 0 0;
				padding-left: 1.25rem;
			}
			.cta {
				display: flex;
				gap: 1rem;
				flex-wrap: wrap;
				margin-top: 1.5rem;
			}
			.cta a {
				text-decoration: none;
				padding: 0.5rem 1.25rem;
				border-radius: 999px;
				border: 1px solid rgba(var(--accent), 40%);
				color: rgb(var(--black));
			}
			.cta .primary {
				background: rgb(var(--accent));
				color: white;
			}
			.section {
				margin-top: 3rem;
			}
			.section-header {
				display: flex;
				justify-content: space-between;
				align-items: baseline;
				gap: 1rem;
			}
			.section-header a {
				text-decoration: none;
				color: rgb(var(--accent));
			}
			.grid {
				display: grid;
				grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
				gap: 1.5rem;
				margin-top: 1rem;
			}
			.card {
				display: grid;
				gap: 0.75rem;
				padding: 1.25rem;
				border-radius: 16px;
				border: 1px solid rgba(var(--gray), 0.2);
				background: white;
				box-shadow: 0 12px 30px rgba(var(--black), 0.08);
				text-decoration: none;
				color: inherit;
				transition: transform 0.2s ease, box-shadow 0.2s ease;
			}
			.card:hover {
				transform: translateY(-4px);
				box-shadow: 0 16px 40px rgba(var(--black), 0.12);
			}
			.card img {
				border-radius: 12px;
			}
			.posts {
				list-style: none;
				margin: 1rem 0 0;
				padding: 0;
				display: grid;
				gap: 1rem;
			}
			.posts a {
				display: grid;
				gap: 0.35rem;
				padding: 1rem;
				border-radius: 12px;
				border: 1px solid rgba(var(--gray), 0.2);
				text-decoration: none;
				color: inherit;
			}
			.posts span {
				font-size: 0.85rem;
				color: rgb(var(--gray));
			}
		</style>
	</body>
</html>
